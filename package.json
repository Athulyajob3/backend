 {
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT",
    "dev": "json-server --watch db.json --port 5000"
  }
}
